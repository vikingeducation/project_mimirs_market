<h1>My Shopping Cart</h1>
{{#if nothing}}
<h1>Your cart is empty</h1>
{{else}}
{{#each result as |product|}}
  <div class="row mb-3">
    <div class="col-sm">
      <img src="/images/{{ photoId product.id}}.jpg" style="width:320px;" alt="">
    </div>
    <div class="col-sm">
      <h2>{{product.name}}</h2>
      <h5>{{product.Category.name}}</h5>
      <h2>${{product.price}}.00</h2>
    </div>
    <form class="col-sm" action="/mycart/updateitem" method="post">

      <input type="number" name="quantity" value="{{product.quantity}}">

      <input type="hidden" name="id" value="{{product.id}}">

      <input class="btn btn-primary" type="submit" name="" value="Update quantity">
      <a class="btn btn-danger" href="/clear/{{product.id}}">Clear From Cart</a>
    </form>
  </div>
{{/each}}

<div class="row">
  <h1 class="mr-3" >Total: ${{total}}</h1>
  <a class="btn btn-danger mr-3" href="/mycart/delete">Clear Cart</a>
  <a class="btn btn-primary mr-3" href="/checkout">Checkout</a>
</div>
{{/if}}
