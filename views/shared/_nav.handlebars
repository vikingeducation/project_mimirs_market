<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-nav-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Mimir's Market {{#if title }} | {{ title }} {{/if }}</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="main-nav-collapse">
      <form action="{{ productsPath }}" class="navbar-form navbar-left">
        <section class="form-group">
          <input type="text" name="search" placeholder="Search..." class="form-control">
        </section>
        <button type="submit" class="btn btn-default">Go</button>
      </form>
      <ul class="nav navbar-nav">
        <li><a href="{{ productsPath }}">Products</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="{{ cartPath }}">View Cart</a></li>
        <li><a href="{{ checkoutPath }}">Check Out</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ cartProducts.length }} Poducts <span class="caret"></span></a>
          <ul class="dropdown-menu">
            {{#unless cartProducts}}
            <li><a href="#">Your cart is empty...</a></li>
            {{/unless}} {{#each cartProducts as |product| }}
            <li><a href="{{ productPath product.id }}">{{ product.count }} {{ product.name }}</a></li>
            {{/each}}
          </ul>
        </li>
      </ul>

    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>
