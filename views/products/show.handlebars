<header class="page-header">
 <h1>Product</h1>
</header>

<div class="row">
  <div class="col-xs-3">
      <img src="/images/{{currentProduct.photo}}">
  </div>
  <div class="col-xs-3">
      <p>{{currentProduct.name}}</p>
      <p>{{currentProduct.Category.name}}</p>
      <p>{{currentProduct.price}}</p>
  </div>
  <div class="col-xs-6">
      <p>{{currentProduct.description}}</p>
      <form>
        <input type="hidden" value="{{currentProduct.id}}">
        </a><button type="submit" class="btn btn-primary">Add to Cart</button>
      </form>
  </div>

</div>

<div class="row">
  <div class="col-xs-12 text-center">
    <h2>Related Products:</h2>
  </div>
</div>
<div class="clearfix"></div>
<div class="row">
 {{#each products as |product|}}
    <div class="col-xs-4 index-img">
      <img src="/images/{{product.photo}}" /><br>
      <div class="text-center">
        <p><a href="/products/{{product.id}}">{{product.name}}</a></p>
        <p>{{product.price}}</p>
        <p>{{../currentProduct.Category.name}}</p>
        <button type="submit" class="btn btn-primary">Add to Cart</button>
      </div>
    </div>
  {{/each}}
</div>
<div class="clearfix"></div>
