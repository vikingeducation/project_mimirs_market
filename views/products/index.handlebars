<header class="page-header">
  <h1>Your Products</h1>
</header>
<!--
name: DataTypes.STRING,
sku: DataTypes.INTEGER,
description: DataTypes.STRING,
price: DataTypes.INTEGER,
categoryId: DataTypes.INTEGER, -->


<h2>Search</h2>
<form action="/products">


  {{!-- Price--}}
  <h3>Price</h3>
  <select class="form-control" name="price[min]" id="price_min">
    {{#times start=0 stop=1000000 as |prices| }}
      <option value="{{ prices }}" {{#if (eq searchSettings.price.min prices) }}selected{{/if }}>{{ feetprices prices }}</option>
    {{/times}}
  </select>
  <br>
  <select class="form-control" name="height[max]" id="height_max">
    {{#times start=0 stop=1000000 as |prices| }}
      <option value="{{ prices }}" {{#if (eq searchSettings.price.max prices) }}selected{{/if }}>{{ feetprices prices }}</option>
    {{/times}}
  </select>

  {{!-- Product Type --}}
  <h3>Category Type</h3>
  {{#each categoryTypes as |categoryType| }}
    <div class="checkbox">
      <label for="category_types_{{ productType }}">
        <input type="checkbox" name="category_types[]" id="category_types_{{ categoryType }}" value="{{ categoryType }}" {{#if (lookup ../searchSettings.categoryTypes categoryType) }}checked{{/if }}> {{ categoryType }}
      </label>
    </div>
  {{/each }}


  <div class="form-group">
    <input type="submit" class="btn btn-primary">
  </div>
</form>


{{#if products.length }}
  <div id="search-results" class="row">
    {{#each products as |product| }}
      <div class="user col-xs-12 col-sm-6 col-md-4 text-center">
        <div class="thumbnail">
          <img src="{{ productImagePath product }}" alt="User image" class="img-responsive">
          <div class="caption">
            <h3>{{ product.name }}</h3>
            <p>
              <a href="{{ productPath product.id }}" class="btn btn-default" role="button">Details</a>
            </p>
          </div>
        </div>
      </div>
    {{/each }}
  </div>
{{else }}
  <p class="text-danger">No products</p>
{{/if }}
