<header class="page-header">
  <h1>Your Products</h1>
</header>
<!--
name: DataTypes.STRING,
sku: DataTypes.INTEGER,
description: DataTypes.STRING,
price: DataTypes.INTEGER,
categoryId: DataTypes.INTEGER, -->


<h2>Search</h2>
<form action="/products">


  {{!-- Price--}}
  <h3>Price</h3>
  <select class="form-control" name="price[min]" id="price_min">
    {{#times start=48 stop=84 as |inches| }}
      <option value="{{ inches }}" {{#if (eq searchSettings.price.min inches) }}selected{{/if }}>{{ feetInches inches }}</option>
    {{/times}}
  </select>
  <br>
  <select class="form-control" name="height[max]" id="height_max">
    {{#times start=48 stop=84 as |inches| }}
      <option value="{{ inches }}" {{#if (eq searchSettings.price.max inches) }}selected{{/if }}>{{ feetInches inches }}</option>
    {{/times}}
  </select>

  {{!-- Product Type --}}
  <h3>Product Type</h3>
  {{#each productTypes as |bodyType| }}
    <div class="checkbox">
      <label for="product_types_{{ bodyType }}">
        <input type="checkbox" name="product_types[]" id="product_types_{{ bodyType }}" value="{{ bodyType }}" {{#if (lookup ../searchSettings.productTypes bodyType) }}checked{{/if }}> {{ bodyType }}
      </label>
    </div>
  {{/each }}


  <div class="form-group">
    <input type="submit" class="btn btn-primary">
  </div>
</form>


{{#if products.length }}
  <div id="search-results" class="row">
    {{#each products as |user| }}
      <div class="user col-xs-12 col-sm-6 col-md-4 text-center">
        <div class="thumbnail">
          <img src="{{ userImagePath user }}" alt="User image" class="img-responsive">
          <div class="caption">
            <h3>{{ product.name }}</h3>
            <p>
              <a href="#" class="btn btn-primary" role="button">Like</a>
              <a href="{{ userPath user.id }}" class="btn btn-default" role="button">View</a>
            </p>
          </div>
        </div>
      </div>
    {{/each }}
  </div>
{{else }}
  <p class="text-danger">No products</p>
{{/if }}
